name: CI Release

# Only runs on pushes to main — handles VERSION-gated release flow
on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: read

jobs:
  pre-checks:
    name: pre-checks (version, permission)
    runs-on: ubuntu-latest
    outputs:
      version_changed: ${{ steps.changes.outputs.version_changed }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Detect VERSION change
        id: changes
        shell: bash
        run: |
          set -euo pipefail

          before="${{ github.event.before }}"
          head="${{ github.sha }}"

          if [ "$before" = "0000000000000000000000000000000000000000" ] || [ "$before" = "$head" ]; then
            changed=""
          else
            changed="$(git diff --name-only "$before" "$head" || true)"
          fi

          version_changed=false
          if echo "$changed" | grep -q '^VERSION$'; then
            version_changed=true
          fi

          echo "version_changed=$version_changed" >> "$GITHUB_OUTPUT"

      - name: Enforce patch bump permission (direct main push)
        if: steps.changes.outputs.version_changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          user="${{ github.actor }}"
          perm="$(curl -fsSL \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ github.repository }}/collaborators/$user/permission" \
            | jq -r '.permission')"
          case "$perm" in
            admin|maintain|write) echo "OK: $user has $perm access";;
            *) echo "VERSION bumps on main require write access; $user has permission: $perm"; exit 1 ;;
          esac

      - name: Auto bump build version
        if: steps.changes.outputs.version_changed != 'true'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          python -m pip install --upgrade pip
          python -m pip install bumpver
          make bump BUMP_TYPE=build
          if git diff --quiet HEAD -- Cargo.lock; then
            echo "Cargo.lock unchanged — not amending"
          else
            git add Cargo.lock
            git commit --amend --no-edit
          fi
          git push origin main

  build:
    name: build (release)
    runs-on: ubuntu-latest
    needs: [pre-checks]
    if: needs.pre-checks.outputs.version_changed == 'true'
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GH_PAT }}
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache crates.io index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry/index
          key: crates-io-index-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: crates-io-index-${{ runner.os }}-
      - name: Cache cargo install tools
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: cargo-install-tools-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-install-tools-${{ runner.os }}-
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - uses: Swatinem/rust-cache@v2
      - run: echo "VERSION=$(cat VERSION)" >> "$GITHUB_ENV"
      - run: cargo build --release
      - run: mv target/release/certboy "certboy-${VERSION}-x86_64-unknown-linux-gnu"
      - run: cp VERSION STABLE.txt
      - uses: actions/upload-artifact@v4
        with:
          name: certboy-${{ env.VERSION }}-gnu
          path: |
            certboy-${{ env.VERSION }}-x86_64-unknown-linux-gnu
            STABLE.txt

  build-musl:
    name: build (x86_64-unknown-linux-musl)
    runs-on: ubuntu-latest
    needs: [pre-checks]
    if: needs.pre-checks.outputs.version_changed == 'true'
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GH_PAT }}
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache crates.io index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry/index
          key: crates-io-index-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: crates-io-index-${{ runner.os }}-
      - name: Cache cargo install tools
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: cargo-install-tools-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-install-tools-${{ runner.os }}-
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update -qq && sudo apt-get install -y -qq musl-tools
      - run: rustup target add x86_64-unknown-linux-musl
      - run: echo "VERSION=$(cat VERSION)" >> "$GITHUB_ENV"
      - run: cargo build --release --target x86_64-unknown-linux-musl
      - run: mv target/x86_64-unknown-linux-musl/release/certboy "certboy-${VERSION}-x86_64-unknown-linux-musl"
      - run: cp VERSION STABLE.txt
      - uses: actions/upload-artifact@v4
        with:
          name: certboy-${{ env.VERSION }}-musl
          path: |
            certboy-${{ env.VERSION }}-x86_64-unknown-linux-musl
            STABLE.txt

  build-darwin:
    name: build (aarch64-apple-darwin)
    runs-on: macos-latest
    needs: [pre-checks]
    if: needs.pre-checks.outputs.version_changed == 'true'
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GH_PAT }}
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache crates.io index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry/index
          key: crates-io-index-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: crates-io-index-${{ runner.os }}-
      - name: Cache cargo install tools
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: cargo-install-tools-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-install-tools-${{ runner.os }}-
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - uses: Swatinem/rust-cache@v2
      - run: rustup target add aarch64-apple-darwin
      - run: echo "VERSION=$(cat VERSION)" >> "$GITHUB_ENV"
      - run: cargo build --release --target aarch64-apple-darwin
      - run: mv target/aarch64-apple-darwin/release/certboy "certboy-${VERSION}-aarch64-apple-darwin"
      - run: cp VERSION STABLE.txt
      - uses: actions/upload-artifact@v4
        with:
          name: certboy-${{ env.VERSION }}-darwin
          path: |
            certboy-${{ env.VERSION }}-aarch64-apple-darwin
            STABLE.txt

  build-arm-linux:
    name: build (aarch64-unknown-linux-gnu)
    runs-on: ubuntu-latest
    needs: [pre-checks]
    if: needs.pre-checks.outputs.version_changed == 'true'
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GH_PAT }}
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache crates.io index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry/index
          key: crates-io-index-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: crates-io-index-${{ runner.os }}-
      - name: Cache cargo install tools
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: cargo-install-tools-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-install-tools-${{ runner.os }}-
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - uses: Swatinem/rust-cache@v2
      - run: rustup target add aarch64-unknown-linux-gnu
      - run: echo "VERSION=$(cat VERSION)" >> "$GITHUB_ENV"
      - run: cargo build --release --target aarch64-unknown-linux-gnu
      - run: mv target/aarch64-unknown-linux-gnu/release/certboy "certboy-${VERSION}-aarch64-unknown-linux-gnu"
      - run: cp VERSION STABLE.txt
      - uses: actions/upload-artifact@v4
        with:
          name: certboy-${{ env.VERSION }}-arm-gnu
          path: |
            certboy-${{ env.VERSION }}-aarch64-unknown-linux-gnu
            STABLE.txt

  build-arm-linux-musl:
    name: build (aarch64-unknown-linux-musl)
    runs-on: ubuntu-latest
    needs: [pre-checks]
    if: needs.pre-checks.outputs.version_changed == 'true'
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GH_PAT }}
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache crates.io index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry/index
          key: crates-io-index-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: crates-io-index-${{ runner.os }}-
      - name: Cache cargo install tools
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: cargo-install-tools-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-install-tools-${{ runner.os }}-
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update -qq && sudo apt-get install -y -qq musl-tools
      - run: rustup target add aarch64-unknown-linux-musl
      - run: echo "VERSION=$(cat VERSION)" >> "$GITHUB_ENV"
      - run: cargo build --release --target aarch64-unknown-linux-musl
      - run: mv target/aarch64-unknown-linux-musl/release/certboy "certboy-${VERSION}-aarch64-unknown-linux-musl"
      - run: cp VERSION STABLE.txt
      - uses: actions/upload-artifact@v4
        with:
          name: certboy-${{ env.VERSION }}-arm-musl
          path: |
            certboy-${{ env.VERSION }}-aarch64-unknown-linux-musl
            STABLE.txt

  release:
    name: GitHub Release & Crates.io Publish
    runs-on: ubuntu-latest
    needs: [pre-checks, build, build-musl, build-darwin, build-arm-linux, build-arm-linux-musl]
    if: needs.pre-checks.outputs.version_changed == 'true'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Tag and GitHub release
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ ! -f VERSION ]; then
            echo "ERROR: VERSION file not found"
            exit 1
          fi
          VERSION=$(cat VERSION)
          if [ -z "$VERSION" ]; then
            echo "ERROR: VERSION file is empty"
            exit 1
          fi
          tag="v${VERSION}"

          if [ ! -d dist ] || [ -z "$(ls -A dist/ 2>/dev/null)" ]; then
            echo "ERROR: dist/ directory empty or missing"
            exit 1
          fi

          git fetch --tags --force
          if git ls-remote --tags origin "refs/tags/$tag" | grep -q "$tag"; then
            echo "Tag already exists: $tag — skipping"
          else
            git tag -a "$tag" -m "$tag"
            git push origin "$tag"
          fi

          # Generate changelog for this release via git-cliff
          pip install --quiet git-cliff
          # Use --current to process commits belonging to the current tag (the one just pushed)
          if git-cliff --current --output /tmp/cliff_notes.md 2>&1; then
            echo "Generated changelog via git-cliff for current tag"
          else
            echo "WARNING: git-cliff failed — using basic release notes"
            echo "Release $tag" > /tmp/cliff_notes.md
          fi

          gh release create "$tag" dist/* --title "$tag" --notes-file /tmp/cliff_notes.md

      - name: Publish to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Cargo.lock
          git diff --cached --quiet || git commit -m "chore: update Cargo.lock"
          cargo publish
